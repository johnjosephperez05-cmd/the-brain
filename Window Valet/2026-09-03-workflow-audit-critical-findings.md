# Window Valet — Workflow Cleanup & Documentation audit, critical findings (2026-09-03)

Reviewed a detailed workflow inventory/audit PDF the user produced (`the window valet/2026-09-02_audit_week3-4-workflow-inventory.pdf`, dated 2026-09-05 in the doc itself, likely a forward-planning label — treat 2026-09-03 as the actual review date). 85 workflows total, 41 published, 44 Draft, 43 sitting in a single "6. Inactive Automations" folder that turns out to be a near-complete second copy of the account (documented in detail in that PDF; this note captures only the parts requiring urgent action or that change prior assumptions).

## Urgent, not yet acted on
- **Live MyBlindCo webhook is actively failing.** "My Blind Co, GHL Erica" (the current one-way GHL→MyBlindCo sync) has failed webhook calls for at least two real contacts: Brett Driscoll (2026-08-28) and Nicholas Carey (2026-08-24). Flagged on GHL's own "Needs Review" list. Real, ongoing data not reaching MyBlindCo right now, directly relevant to the Customer Journey & Cross-System Integration project.
- **Possible gap in the "1. New Lead" existing-customer fix.** A second, separate workflow, "01. New Lead Opt in Workflow" (sitting in the legacy "Inactive Automations/Lead Flow Pipeline" folder despite being Published), is still actively fed live traffic directly by both "Google Ad (Exterior) to GHL" and "Google Ads (home page) to GHL" (via explicit Add-to-Workflow, bypassing "1. New Lead" entirely), and likely also by "Phone Number Tracking." If this workflow lacks the same existing-customer exclusion added to "1. New Lead" this week, Google Ads-sourced leads specifically may still be vulnerable to the false-new-lead bug we thought was fixed. Needs checking before calling that bug closed.
- ~~**Confirmed live duplicate: "8. Cancelled Appointment" vs. "Appointment Cancellation Workflow."**~~ **Resolved 2026-09-03.** Both were Published, both fired on the same appointment-cancelled event; builds had diverged (different opening channel, touch count, and ending: task vs. nurture tag). Fixed by the user same day.
- Mapsly partner sync also failed for 3 records on 2026-08-12: Fischer Homes - Hampton Walk, Fischer Homes - The Townes at Hampton Walk, Lennar - Ravinia (also on GHL's "Needs Review" list).

## Corrects a prior project assumption
The Customer Journey & Cross-System Integration project assumed GHL already tracks order/install status (Production Complete, Order Shipped, Installation Scheduled, etc. — the "Customer Order Flow Pipeline" stages). It does not: all nine of that pipeline's workflows are sitting in Draft inside the inactive folder. Nothing on the GHL side is actually tracking order/install status today. Relevant to the data-map/architecture work for that project.

## Other confirmed findings from the audit
- "My Blind Co - GHL Justin" is Draft but has 308 past enrollments — was live before being reverted. Structurally near-identical to the still-Published "My Blind Co - GHL Erica." Real decision needed from Josh: republish Justin's copy as a parallel per-salesperson sync, or consolidate into one shared MyBlindCo sync not tied to a specific salesperson.
- Two Draft workflows still have real, currently-active contacts inside them: "Appointment Booked Workflow" (legacy, 19 active) and "Drip Campaign Workflow" (legacy, 2 active). Those contacts need accounting for before either workflow gets touched or archived.
- The "two mismatched appointment reminder emails" anomaly (found earlier this week) has a likely explanation: "1. Appointment Booked"'s own builder left a note that its reminder sequence is duplicated four times, once per lead source, instead of one shared sequence — drift between the four copies is a simpler explanation than two disagreeing workflows. Still needs the four copies compared directly to confirm.
- "7. Not Booked Workflow" is live, in the current Sales Pipeline folder, but its own trigger watches a pipeline called "Lead Flow Pipeline" (the legacy pipeline name), not either current pipeline. It's still receiving contacts via direct Add-to-Workflow enrollment from three other lead-source workflows regardless of whether its own trigger ever fires. Needs Josh's confirmation on whether "Lead Flow Pipeline" is still a real, separate pipeline object or dead weight.
- Six live workflows untouched for months (not confirmed broken, just unchecked): Trigger Partner Data Sync (185 days), Map Partner Data to Contact (184 days), all four Partner Communications workflows (102 days each).
- The missing existing-customer-exclusion candidate originally suspected in the roadmap was "New Lead" itself — opened specifically to check, and it turns out to already handle this correctly (checks existing-customer tag first, stops if matched). Ruled out, not confirmed as the bug.
- MyBlindCo has four workflows across two folders, not two: beyond "Erica" (live) and "Justin" (Draft), "GHL to Myblindco" and "My Blind Co - GHL" both sit in the old folder at zero enrollments and look genuinely dead.
- "My Blind Co - GHL Erica" has a second webhook step currently switched off (per the builder's own note) — needs a decision on whether to re-enable or remove it.

## Status of the Week 3-4 Workflow Cleanup & Documentation item itself
Full inventory: done (85 workflows catalogued). Anomalies: documented in depth (see PDF). Sort by function: done, reveals ~8 exact-name duplicates between live and legacy folders plus the Appointment Lifecycle category alone carrying 19 legacy workflows against 6 current. Plain-English descriptions: 34 of 85 done (11 partial). Fix/consolidate overlaps: not started, waiting on Josh's decisions on the candidates above. Archive genuinely-dead workflows: not started, same blocker, plus the two Draft-with-active-people workflows need resolving first. Reference index: being assembled progressively via the inventory + descriptions sections. Recurring review cadence: not yet set, needs Josh's input (quarterly suggested).
